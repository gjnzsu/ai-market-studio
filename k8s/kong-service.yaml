apiVersion: v1
kind: Service
metadata:
  name: ai-gateway-kong
  namespace: ai-gateway
  labels:
    app: ai-gateway-kong
spec:
  type: ClusterIP
  selector:
    app: ai-gateway-kong
  ports:
    - name: http
      port: 80
      targetPort: 8000

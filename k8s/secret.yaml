# TEMPLATE ONLY — do NOT commit real values.
# Create the secret imperatively instead:
#
#   kubectl create secret generic ai-market-studio-secrets \
#     --from-literal=OPENAI_API_KEY=<your-openai-key> \
#     --from-literal=EXCHANGERATE_API_KEY=<your-key-or-dummy>
#
# If you must apply this file, replace the placeholders below with
# base64-encoded values:  echo -n 'value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: ai-market-studio-secrets
type: Opaque
data:
  OPENAI_API_KEY: REPLACE_WITH_BASE64_ENCODED_VALUE
  EXCHANGERATE_API_KEY: REPLACE_WITH_BASE64_ENCODED_VALUE
